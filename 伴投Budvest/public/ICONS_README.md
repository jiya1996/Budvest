# PWA Icons

为了完整的PWA体验，请在 `public/` 目录下添加以下图标文件：

- `icon-192.png` (192x192 pixels)
- `icon-512.png` (512x512 pixels)  
- `favicon.ico` (标准favicon)

这些图标将用于：
- PWA "添加到主屏幕"功能
- 浏览器标签页图标
- 移动设备主屏幕图标

**注意**: 如果没有这些图标文件，PWA功能可能受限，但应用仍可正常运行。
