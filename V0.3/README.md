# Investbuddy - 投资心理陪伴与复盘工具 (V0.3)

> 一个结合 AI 心理辅导与多市场数据支持的智能投资伴侣 (PWA)。

![Version](https://img.shields.io/badge/version-0.3.0-blue)
![Stack](https://img.shields.io/badge/tech-Next.js%2015%20%7C%20Python%20%7C%20SQLite-green)

## 🌟 核心特性 V0.3

### 1. 双核市场数据引擎
- **美股/港股 (Global)**: 集成 Financial Modeling Prep (FMP) API，提供全球市场实时数据。
- **A 股 (Local)**: 内置 Python 数据采集服务 (基于 AkShare)，本地 SQLite 数据库支持 A 股实时行情、K线、资金流向等深度数据。

### 2. 进化版 AI 投资导师
- **7 位传奇导师**: 巴菲特、索罗斯、达利欧等不同风格的 AI 角色。
- **RAG 增强**: 基于向量数据库 (Vector DB) 的检索增强生成，引用大师原著与经典案例。
- **长期记忆系统**: 记住用户的投资偏好、历史情绪与对话上下文 (SQLite 持久化)。
- **情绪识别**: 自动分析用户焦虑、贪婪等心理状态并提供针对性疏导。

### 3. 全功能投资系统
- **复盘日记**: 记录交易决策与心路历程，支持情绪打标。
- **组合管理**: 实时追踪持仓盈亏，支持多市场资产。
- **PWA 支持**: 完美的移动端体验，可安装至手机主屏幕。

## 🏗️ 系统架构

本项目采用 **混合架构 (Hybrid Architecture)**：

- **Frontend (App)**: Next.js 15 (App Router) + TypeScript + TailwindCSS
- **Backend (Data)**: Python Data Service (AkShare ETL) + SQLite
- **Intelligence**: OpenAI GPT-4o-mini + RAG/Memory System

## 🚀 快速开始

### 前置要求
- Node.js 18+
- Python 3.8+
- API Keys: OpenAI API Key, FMP API Key (可选，用于美股数据)

### 第一步：启动数据服务 (Data Service)

数据服务负责采集 A 股数据并维护本地数据库。

```bash
# 进入数据服务目录
cd data-service

# 安装 Python 依赖
pip install -r requirements.txt

# 初始化数据库 (自动创建 ../data/investbuddy.db)
python database.py

# 启动采集与调度服务
python run.py
```

> **注意**: 保持此终端开启，或者在后台运行。

### 第二步：启动应用 (Web App)

```bash
# 回到项目根目录
cd ..

# 安装 Node 依赖
npm install

# 配置环境变量
# 复制 .env.example 到 .env.local 并填入你的 API Key
cp .env.example .env.local
```

编辑 `.env.local`:
```env
FMP_API_KEY=your_key
OPENAI_API_KEY=your_key
```

启动开发服务器:
```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 即可使用。

## 📂 项目结构

```
investbuddy/
├── app/                    # Next.js 应用主逻辑
│   ├── api/               # API 路由 (Chat, Market, Portfolio)
│   ├── dashboard/         # 仪表板
│   ├── chat/             # AI 对话界面
│   └── ...
├── components/            # React UI 组件
├── data/                  # 本地数据存储 (SQLite DB 所在位置)
├── data-service/          # [新增] Python 数据采集微服务
│   ├── collectors/       # 各类数据采集脚本 (行情, 新闻, 资金等)
│   ├── database.py       # 数据库模型定义
│   └── run.py            # 服务启动入口
├── lib/                   # 核心工具库
│   ├── db.ts             # SQLite 连接与查询封装
│   ├── rag.ts            # [新增] RAG 知识库模块
│   ├── memory.ts         # [新增] AI 长期记忆模块
│   └── ...
└── public/                # 静态资源 (PWA Icons, Manifest)
```

## 🛠️ 技术栈详情

- **框架**: Next.js 15, React 19
- **样式**: TailwindCSS, Lucide Icons
- **数据库**: SQLite (via `better-sqlite3`)
- **数据源**:
  - AkShare (A股, 免费开源)
  - FMP (美股, API)
- **AI**: OpenAI SDK, Local Vector Search (Cosine Similarity)

## 📝 开发计划

- [x] A股数据本地化采集
- [x] AI 长期记忆与 RAG
- [ ] 更多市场数据指标 (财务分析)
- [ ] 用户认证系统 (Auth.js)
- [ ] 多语言支持

## 许可证

MIT License
